node_modules
.next
.env.local
.env
.DS_Store
*.log
coverage
.vercel


# Build artifacts
tsconfig.tsbuildinfo

# Local backups
.backup/
.backups/
.local-backups/
.backup-*.tsx

# Supabase CLI local temporary files
supabase/.temp/

# Stray archives that shouldn't be committed
*.tar.gz
*.zip

# Machine-local Claude Code settings (permission grants, not shared config)
.claude/settings.local.json
