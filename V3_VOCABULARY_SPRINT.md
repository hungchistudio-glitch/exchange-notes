# Exchange Notes V3 — Vocabulary Sprint

## Completed

- Refactored the vocabulary page to use the shared `AppPage` layout.
- Rebuilt the vocabulary header with the global `PageHeader` component.
- Added a focused daily-review hero with clear progress and two primary actions.
- Added reusable `SectionHeader`, `AppBadge`, and `EmptyState` UI primitives.
- Added shared CSS tokens and vocabulary-specific design primitives to `app/globals.css`.
- Preserved existing Supabase, Gemini, pronunciation, filtering, sorting, sharing, and review functionality.

## Verification

`npm run build` completed successfully with all 28 pages generated.
